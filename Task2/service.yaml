apiVersion: v1
kind: Service
metadata:
  name: insuretech-app-svc
  namespace: default
  labels:
    app: insuretech-app
spec:
  selector:
    app: insuretech-app
  ports:
    - name: metrics
      port: 80
      targetPort: 8080
  type: NodePort